.row
  h4 Neo4j Arc
  p You are logged in on Neo4j Arc as:
    br
    code {{CurrentUser._user.name}} ({{CurrentUser._user.identities[0].provider}})
.row(style="margin-top: 10px")
  button.btn.btn-default(ng-click="CurrentUser.logout()") Logout
